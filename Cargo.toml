[package]
name = "main"
version = "0.1.0"
edition = "2021"
default-run = "main"

[dependencies]
macroquad = { version = "0.4", features = ["audio"] }

[lib]
name = "i8080_emulator"
path = "src/i8080.rs"


[profile.release]
opt-level = 3
lto = true
codegen-units = 1  # improves LTO effectiveness
